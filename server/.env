OPENAI_API_KEY = "aqui deveria ir tu llave"

// esto es un comentario:
en "segmento variable de "./server.js" 
se debe modificar con:

const ai = new GoogleGenAI({
    apiKey: process.env.GEMINI_API_KEY,
});

en caso de usar la llave en este archivo